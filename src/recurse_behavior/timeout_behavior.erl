-module(timeout_behavior).

% Declare timeout values and error tolerance for recursing

% We only need resend for udp resolvers...

% Timeout based on what we know about the average latency for that address?
